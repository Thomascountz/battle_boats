#!/usr/bin/env ruby

require "battle_boats/engine"
require "battle_boats/fleet"
require "battle_boats/random_fleet_deployer"

fleet = BattleBoats::Fleet.new
board = BattleBoats::RandomFleetDeployer.new.deploy(fleet)

engine = BattleBoats::Engine.new(board: board)
engine.start

