#!/usr/bin/env ruby

require 'battle_boats/engine'
require 'battle_boats/board'

board = BattleBoats::Board.new
board.place_ships_randomly

engine = BattleBoats::Engine.new(enemy_board: board)
engine.deploy_ally_ships
engine.start

